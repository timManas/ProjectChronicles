{
  "name": "chronicles",
  "version": "1.0.0",
  "description": "FrontEnd: React <br/> BackEnd: Node/Express <br/> DB: MongoDB <br/>",
  "main": "index.js",
  "scripts": {
    "backend": "npm run server --prefix backend",
    "frontend": "npm run client --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npm install --prefix frontend --production=false && npm run build --prefix frontend  && npm install --prefix backend && npm run build --prefix backend"
  },
  "author": "Tim Manas",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "framer-motion": "^11.0.5"
  }
}
